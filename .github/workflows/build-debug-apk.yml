name: Build Debug APK

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Assemble debug APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Accept Android SDK licenses
        run: yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null || true

      - name: Make gradlew executable
        run: chmod +x gradlew

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4

      - name: Build debug APK
        run: ./gradlew assembleDebug --stacktrace

      - name: Copy debug APK to project root
        # M1 requirement: deliverable debug APK available at the repository root.
        run: cp app/build/outputs/apk/debug/app-debug.apk pulse-debug.apk

      - name: Upload debug APK (project root)
        uses: actions/upload-artifact@v4
        with:
          name: pulse-debug-apk
          path: pulse-debug.apk
          if-no-files-found: error
          retention-days: 14
